# One-batch pipeline smoke only. Random initialization avoids external cache
# access; this run is not model evidence and must not be resumed or promoted.

[data]
dataset_root = "ml/data/raw/orthodontic_plaque/v3/extracted/part-2/mendeley-dataset-materials_Part_2"
manifest_path = "ml/data/prepared/orthodontic_plaque/v4/part-2/manifest.csv"

[model]
num_classes = 2
image_min_size = 512
image_max_size = 768
trainable_backbone_layers = 3
pretrained_weights = "none"

[training]
epochs = 1
batch_size = 1
learning_rate = 0.005
momentum = 0.9
weight_decay = 0.0005
num_workers = 0
device = "auto"
seed = 20260711
max_train_batches = 1
max_validation_batches = 1

[output]
output_dir = "ml/runs/detection/orthodontic_plaque_part2_mvp_v4_smoke"
